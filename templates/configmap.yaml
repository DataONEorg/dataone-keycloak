# Load all files in the "config" directory into a ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: imports
  labels:
    {{- include "keycloak-dataone.labels" . | nindent 4 }}
data:
{{ (tpl (.Files.Glob "imports/*").AsConfig . ) | indent 4 }}
